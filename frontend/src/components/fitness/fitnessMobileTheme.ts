/** Apple Fitness–inspired mobile tokens (adapted for Innerarc #39FF14). */
export const FITNESS_MOBILE = {
  ringSize: 200,
  ringStroke: 18,
  tabBarHeight: 62,
  tabBarRadius: 28,
  heroCardRadius: 20,
  listIconSize: 22,
} as const;

export const WEEK_DAYS = ["M", "T", "W", "T", "F", "S", "S"] as const;
